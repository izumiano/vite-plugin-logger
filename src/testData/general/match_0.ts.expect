import {trace, logError} from "@izumiano/vite-logger"

logError("ERROR");

class ClassName{
	public functionName(){
		trace("%c[ClassName] %c[functionName]", "Message");
		logError("%c[ClassName] %c[functionName]", "Error Message");
	}
}